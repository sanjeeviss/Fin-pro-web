import { theme } from "antd";

export const LightThemeConfig = {
    token: {
      colorPrimary: "#797BCF",
      colorSecondary: "#B7B8DC",
      siderBG: "#fff",
    },
    algorithm: theme.defaultAlgorithm,
  };